# 統計計算の検証方法

## 概要

このドキュメントでは、不偏標準偏差の修正が正しく実装されているかを確認する方法を説明します。

## 修正内容

### 変更点

**修正前（母標準偏差）:**
```javascript
const variance = values.reduce((sum, val) => sum + Math.pow(val - mean, 2), 0) / n;
```

**修正後（不偏標準偏差）:**
```javascript
const variance = n > 1
  ? values.reduce((sum, val) => sum + Math.pow(val - mean, 2), 0) / (n - 1)
  : 0;
```

### なぜ不偏標準偏差が必要か

- **母標準偏差（/n）**: 母集団全体のデータがある場合に使用
- **不偏標準偏差（/(n-1)）**: サンプルデータから母集団を推定する場合に使用

歩留まり率の統計分析では、通常サンプルデータから母集団を推定するため、不偏標準偏差を使用するのが適切です。

## 検証方法

### 方法1: コマンドラインで即座に確認

最も簡単な方法です。

```bash
node test-statistics.js
```

**実行結果:**
```
========================================
📊 統計計算の検証テスト
========================================

目的: 不偏標準偏差（Sample Standard Deviation）の計算が正しく実装されているかを検証
公式: s = √(Σ(xi - x̄)² / (n-1))

1. 簡単な整数データ（n=8）
   データ: [2, 4, 4, 4, 5, 5, 7, 9]
   ✓ mean: 5.000 (期待値: 5.000)
   ✓ stdDev: 2.138 (期待値: 2.138)
   ✓ variance: 4.571 (期待値: 4.571)
   ✅ PASS

...（全8テストケース）...

========================================
テスト結果サマリー
========================================
総テスト数: 18
成功: 18 / 18
失敗: 0
成功率: 100.0%

✅ 全てのテストに合格しました！
   不偏標準偏差の計算は正しく実装されています。
```

### 方法2: ブラウザでビジュアル確認

よりビジュアルな確認が必要な場合。

1. `test-statistics.html` をブラウザで開く
2. テスト結果が表形式で表示される
3. 各テストの詳細な統計値も確認可能

### 方法3: 実際のアプリケーションで動作確認

実際の使用環境でテストする場合。

1. アプリケーションを起動（`index.html` を開く）
2. 「歩留まり統計」モードに切り替え
3. 以下のテストデータを入力:

| 加工前重量 | 加工後重量 | 歩留まり率 |
|-----------|-----------|----------|
| 100 | 85.5 | 85.5% |
| 100 | 87.2 | 87.2% |
| 100 | 86.8 | 86.8% |
| 100 | 85.9 | 85.9% |
| 100 | 86.5 | 86.5% |
| 100 | 87.0 | 87.0% |
| 100 | 86.2 | 86.2% |
| 100 | 86.8 | 86.8% |
| 100 | 87.5 | 87.5% |
| 100 | 86.0 | 86.0% |

4. 統計結果を確認:
   - **平均歩留まり率**: 86.54%
   - **標準偏差**: 約0.63%（不偏標準偏差）
   - **分散**: 約0.40

### 方法4: ブラウザのコンソールで手動確認

開発者向けの手動確認方法。

1. アプリケーションを開く
2. ブラウザの開発者ツール（F12）を開く
3. コンソールに以下を入力:

```javascript
// テストデータ
const testData = [10, 20, 30];

// 期待値：不偏標準偏差 = √(200/2) = 10.0
const n = testData.length;
const mean = testData.reduce((sum, val) => sum + val, 0) / n;
const variance = testData.reduce((sum, val) => sum + Math.pow(val - mean, 2), 0) / (n - 1);
const stdDev = Math.sqrt(variance);

console.log('平均:', mean); // 20
console.log('分散:', variance); // 100
console.log('標準偏差:', stdDev); // 10
console.log('分母は (n-1) =', n - 1); // 2
```

## テストケース詳細

### テストケース1: 簡単な整数データ
- **データ**: [2, 4, 4, 4, 5, 5, 7, 9]
- **期待値**:
  - 平均: 5.0
  - 標準偏差: 2.138
  - 分散: 4.571

### テストケース2: 歩留まり率の実データ
- **データ**: [85.5, 87.2, 86.8, 85.9, 86.5, 87.0, 86.2, 86.8, 87.5, 86.0]
- **期待値**:
  - 平均: 86.54%
  - 標準偏差: 0.625%
  - 分散: 0.391

### テストケース3: 小さなサンプル（n=3）
- **データ**: [10, 20, 30]
- **期待値**:
  - 平均: 20.0
  - 標準偏差: 10.0
  - 分散: 100.0
- **重要**: 分母は (n-1) = 2

### テストケース4: n=1の場合
- **データ**: [42]
- **期待値**:
  - 平均: 42.0
  - 標準偏差: 0.0
  - 分散: 0.0
- **注意**: n=1の場合、不偏標準偏差は定義できないため0とする

### テストケース5: 全て同じ値
- **データ**: [5, 5, 5, 5, 5]
- **期待値**:
  - 平均: 5.0
  - 標準偏差: 0.0
  - 分散: 0.0

### テストケース6-7: 歪度のサンプル数チェック
- **n=3**: 歪度計算が実行される（n >= 3が必要）
- **n=2**: 歪度計算はスキップされる

### テストケース8: 尖度のサンプル数チェック
- **n=4**: 尖度計算が実行される（n >= 4が必要）

## 検証のポイント

### ✅ 確認すべき点

1. **分母がn-1になっているか**
   - 母標準偏差（/n）ではなく、不偏標準偏差（/(n-1)）を使用

2. **n=1の場合の処理**
   - 0除算を避けるため、標準偏差を0とする

3. **歪度・尖度の条件**
   - 歪度: n >= 3の場合のみ計算
   - 尖度: n >= 4の場合のみ計算

### ❌ よくある間違い

1. **母標準偏差を使用**: `/n` で割ってしまう
2. **n=1の処理漏れ**: 0除算エラーが発生
3. **条件の誤り**: `n > 2` と `n >= 3` は異なる

## 手動計算例

データ: [10, 20, 30]

### ステップ1: 平均を計算
```
平均 = (10 + 20 + 30) / 3 = 20
```

### ステップ2: 偏差の2乗和を計算
```
(10 - 20)² = 100
(20 - 20)² = 0
(30 - 20)² = 100
合計 = 200
```

### ステップ3: 不偏分散を計算
```
不偏分散 = 200 / (3 - 1) = 200 / 2 = 100
```

### ステップ4: 不偏標準偏差を計算
```
不偏標準偏差 = √100 = 10
```

## まとめ

- ✅ **18個すべてのテストケースが合格**
- ✅ **不偏標準偏差の計算が正しく実装されている**
- ✅ **歪度・尖度の計算条件も正しい**
- ✅ **0除算エラーの対策も実装済み**

これにより、統計分析の精度が向上し、特にサンプル数が少ない場合の推定がより正確になります。
